# Build output
build/
/harness
/generator
*.o
*.so
*.a
target/

# Compiled Java helper + generated classpaths (build_baselines.sh + Java adapter build.sh artifacts)
*.class
*.classpath

# Generated workload caches
orders_*.bin
workload/cache/

# Benchmark result files
results/

# Third-party engine sources fetched by build_baselines.sh or by the
# additional_references/<name>_adapter/build.sh scripts.
third_party/

# Python
__pycache__/
*.pyc

# Editors / OS
.DS_Store
*.swp

# Reference canonical text — shipped compressed as .gz; the uncompressed form
# is regenerated locally by `./harness --write-reference` or by gunzip. The
# canonical_output_<scenario>.txt copies are produced by --write-reference on
# non-canonical scenarios and are not committed.
reference/canonical_output.txt
reference/canonical_output_*.txt
CONSISTENCY_REVIEW.md
